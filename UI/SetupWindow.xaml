<Window x:Class="DarkTorch.UI.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DarkTorch Setup" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Select your preferred text editor:" Margin="0,0,0,5"/>
        <ComboBox x:Name="EditorComboBox" Grid.Row="1" Margin="0,0,0,10">
            <ComboBoxItem>Notepad</ComboBoxItem>
            <ComboBoxItem>Notepad++</ComboBoxItem>
            <ComboBoxItem>Visual Studio Code</ComboBoxItem>
        </ComboBox>
        
        <TextBlock Text="Choose color theme:" Grid.Row="2" Margin="0,0,0,5"/>
        <ComboBox x:Name="ThemeComboBox" Grid.Row="3" Margin="0,0,0,10">
            <ComboBoxItem>Light</ComboBoxItem>
            <ComboBoxItem>Dark</ComboBoxItem>
        </ComboBox>
        
        <Button Content="Save" Grid.Row="5" HorizontalAlignment="Right" Width="75" Click="SaveButton_Click"/>
    </Grid>
</Window>
